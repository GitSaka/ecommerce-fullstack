Parfait 👍
On garde **la même rigueur PRO** que pour `categories`.
Voici **LA STRUCTURE DÉFINITIVE de la table `products`** — claire, cohérente, e-commerce ready, **sans confusion**.

---

## ✅ TABLE `products` — VERSION FINALE (RÉFÉRENCE OFFICIELLE)

### 📌 Champs & logique métier

| Champ             | Type      | Description                          |
| ----------------- | --------- | ------------------------------------ |
| id                | INT       | Identifiant unique                   |
| category_id       | INT       | Référence vers `categories.id`       |
| name              | VARCHAR   | Nom du produit                       |
| slug              | VARCHAR   | URL produit (`preservatif-durex-xl`) |
| short_description | VARCHAR   | Description courte (listing)         |
| description       | TEXT      | Description complète                 |
| price             | DECIMAL   | Prix normal                          |
| sale_price        | DECIMAL   | Prix promo (nullable)                |
| stock             | INT       | Quantité disponible                  |
| sku               | VARCHAR   | Référence interne                    |
| image             | VARCHAR   | Image principale                     |
| is_active         | BOOLEAN   | Visible ou non                       |
| created_at        | TIMESTAMP | Création                             |
| updated_at        | TIMESTAMP | Mise à jour                          |

👉 **Cette structure ne change plus** sauf décision business claire.

---

## 🧱 SQL FINAL — MySQL

```sql
CREATE TABLE products (
  id INT AUTO_INCREMENT PRIMARY KEY,

  category_id INT NOT NULL,

  name VARCHAR(150) NOT NULL,
  slug VARCHAR(180) UNIQUE NOT NULL,

  short_description VARCHAR(255),
  description TEXT,

  price DECIMAL(10,2) NOT NULL,
  sale_price DECIMAL(10,2) DEFAULT NULL,

  stock INT DEFAULT 0,
  sku VARCHAR(100),

  image VARCHAR(255),

  is_active BOOLEAN DEFAULT TRUE,

  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    ON UPDATE CURRENT_TIMESTAMP,

  CONSTRAINT fk_products_category
    FOREIGN KEY (category_id)
    REFERENCES categories(id)
    ON DELETE RESTRICT
    ON UPDATE CASCADE
);
```

---

## 🎯 Pourquoi ces choix sont PRO

### 1️⃣ `short_description`

* Utilisée dans :

  * cartes produits
  * pages catégorie
* Évite d’afficher un long texte

---

### 2️⃣ `sale_price`

* Si `NULL` → pas de promo
* Si rempli → prix promo actif
  👉 Logique simple côté frontend :

```js
const finalPrice = product.sale_price ?? product.price
```

---

### 3️⃣ `stock`

* Base pour :

  * “En rupture”
  * “Stock faible”
  * Gestion future (commande)

---

### 4️⃣ `is_active`

* Tu peux cacher un produit sans le supprimer
* Essentiel pour :

  * produits sensibles
  * campagnes terminées

---

## 🔗 Routes frontend que ça permet

* `/products/:slug`
* `/category/:slug`
* `/promotions`
* `/search?q=`

---

## 🧠 Règle qu’on garde

> ❌ Pas de champ inutile
> ✅ Chaque champ a **un usage frontend ou business clair**

---

## 🔜 Prochaine étape logique (choisis)

1️⃣ **SQL FINAL `users` + `orders`**
2️⃣ **API Node.js – CRUD Products**
3️⃣ **Logique panier & commande**
4️⃣ **Admin dashboard (gestion produits)**

👉 Dis-moi **le numéro** et on continue comme des pros 💪



//     try {
//     const formDataToSend = new FormData;
//     formDataToSend.append("name", formData.name);
//     formDataToSend.append("slug", formData.slug);
//     formDataToSend.append("description", formData.description);
//     formDataToSend.append("is_active", formData.is_active);

//     if (formData.image) {
//       formDataToSend.append("image", formData.image);
//     }
//   console.log(formDataToSend)
//     await createCategory(formData);

    // alert("Catégorie créée avec succès");
  } catch (error) {
    console.error(error);
    // alert("Erreur lors de la création");
  }
    // 👉 plus tard : appel API backend
  };










  